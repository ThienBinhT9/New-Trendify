┌─ Messenger.tsx ────────────────────────────────────────────┐
│ activeConversationId = useState (hoặc Redux nếu cần) │
│ │
│ ┌─ ChatSidebar ────────────────────────────────┐ │
│ │ useInfiniteQuery(["conversations"]) │ │
│ │ → auto fetch page 1 │ │
│ │ → fetchNextPage() khi scroll tới cuối │ │
│ │ → data cached, background refetch tự động │ │
│ └───────────────────────────────────────────────┘ │
│ │
│ ┌─ ChatWindow ─────────────────────────────────┐ │
│ │ useInfiniteQuery(["messages", convId]) │ │
│ │ → auto fetch khi convId thay đổi │ │
│ │ → fetchNextPage() = load tin nhắn cũ hơn │ │
│ │ │ │
│ │ useMutation(sendMessage) │ │
│ │ → optimistic update + setQueryData │ │
│ └───────────────────────────────────────────────┘ │
│ │
│ Socket.IO listener: │
│ → queryClient.setQueryData(["messages", convId], ...) │
│ → queryClient.setQueryData(["conversations"], ...) │
└────────────────────────────────────────────────────────────┘
